# 修复交付：FIX-20260916-07

保留当前镜头、敌人数值、XP 修正 2 和命中几何。修复动画空回调、无效速度写入、轻量日志主线程刷盘、死亡后 Static 刚体访问，并处理伤害数字及中文 UI 首次动态字体上传。围栏、烈焰之笔、鬼火及相关提示改用保留 XY 轮廓的平面显示。

## 已完成的技术检查

- 广域 EditMode **791/791**、PlayMode **534/534**；后续空间验证器取样调整的定向 **3/3**，不合并重复计数。
- 同一 07 构建：4K 单人 **720.9027 秒**结算；720p 双端请求后等待选卡约 8.2 秒，**729.0867 秒**结算。两种运行均通过界面重开，清理计数为零。
- 单人 4207 次、双端各 4017 次出生属性无不匹配；完整双端局出生内容一致。
- 单人 135–180 / 180–201 / 240–300 / 333–379 / 599–720.9 秒最大记录帧约 **15.98 / 24.78 / 18.04 / 30.93 / 35.85 ms**；这些区间没有 ≥100 ms 战斗帧。不是全程 144 FPS 保证。
- 独立包放在工程外、带空格路径启动；初始值正确、测试辅助关闭。短局自然死亡于 47.10 秒，记录为失败；正常退出和日志归档完整。
- 随包 495 个清单文件哈希一致；原始人工失败日志与恢复证据哈希未变。

## 验收边界

按用户最新要求停止追加 Computer Use。已取得的画面与日志保留，后续画面表现交给用户人工确认；不为补齐截图重跑全流程。旧版同条件三轮重复对照未完成，因此不声称精确提升比例，也不能将原始 2.34 秒帧完全归因于单一原因。

加载/开局仍有较长帧；FMOD 通道抢占等其他警告保留。当前技术保护、自动输入和确定性选卡记录不作玩法压力结论。

## 人工画面检查（正常游玩时顺便观察）

1. 火圈上下火焰尺度是否自然、相对地图中心是否固定；移动镜头时区分取景变化。
2. 烈焰之笔朝上/下攻击是否仍有异常放大、缩小；鬼火是否有额外拉伸。原美术非对称与尾迹保留。
3. 135–200 秒、285 秒附近和围栏阶段是否仍有明显停顿。

若发现异常，提供版本/运行编号、大致时间及日志包即可；不要求反复截同一画面。

启动 `1-Solo.cmd`；双端用 `2-Host.cmd` 和 `3-Join-Local-Host.cmd`；结束后用 `4-Archive-Latest-Logs.cmd`。详细基线与差异见 README.md，技术数据见 technical-verification.json。

后续仅在必要时使用 Computer Use；每次先说明具体目的并取得用户确认。
