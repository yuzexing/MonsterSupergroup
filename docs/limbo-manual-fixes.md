# 人工试玩问题修复记录

基线：`0117ca00` 统一范围攻击。保留 P6-20260916-02 人工包及原始恢复证据。本次不修改镜头投影、FOV、XP、敌人数值、伤害几何或生成预算。

**当前状态（2026-09-16）：**FIX-20260916-07 已完成同版本单人、双端完整技术流程和界面重开，独立包无辅助入口及日志归档已检查。广域 EditMode 791/791、PlayMode 534/534；其后围栏定向 3/3，不合并重复计数。最终交付摘要见 `docs/evidence/manual-play-fix/acceptance-summary.md`。按用户要求停止追加画面重复验收，旧版三轮对照未完成并明确保留为缺口；不声称原始 2.34 秒帧已获得完整归因。以后只有必要的窗口操作才考虑 Computer Use，须先向用户说明目的并获得当次确认。以下保留带版本的历史记录。

## 原始记录与归因边界

人工样本：`Builds/LimboManual-P6-20260916-02/Runs/20260916-131305-655-solo/host`。实际设置由 720p/60 改为 3840×2160/144。约 168.33 秒采样窗口最大单帧 2336.4 ms，采样存活 26；日志写入另有约 641.9 ms 刷盘。二者不是同一采样，不能直接将全部停顿归因于日志。约 505.25 秒死亡并正常退出，不是已证实的崩溃。

`player.log` 约 74 MB：9448 次空动画事件、8316 次无效 Animator.speed 提示、高频状态机和 XP 文本及调用栈。死亡后对 Static 刚体写速度是独立生命周期缺陷，不用它解释早期长帧。

围栏原版与当前记录均固定中心。原版正交相机下的 45° 倾斜在当前透视相机产生深度放大：半径 10、距离 10 时上下放大比例约 5.8 倍。烈焰之笔与鬼火有相同显示深度问题；鬼火原有拉伸尾迹不等于错误。

## 已实施的修复

- 导入器删除明确未迁移的空音频事件，保留视觉回调、结束标记及原始事件时间。按时间重新绑定，重复执行不会依赖已变化的数组索引。恢复证据不变。
- 敌人移动速度使用 Animancer 层；攻击与死亡解除移动倍率，攻击仍按战斗时间采样状态，不冻结整个图。不关闭 Animancer 警告。
- 轻量模式停止普通状态机/XP 文本及普通文本调用栈，保留警告、错误和战斗证据。
- 现有记录器改为单后台线程顺序写入，主线程不刷盘；4 MiB 有界积压，失败/超限明确标记不完整。每个文件 `.status.json` 仅在成功关闭后标 complete。正常结算/退出仍有限等待刷新；异常退出不宣称日志完整。现有 logWriteMs/logFlushMs 在新包表示后台 I/O 时间，不能与旧包主线程阻塞直接等同。
- 平面显示采用独立 Sprite/VFX Shader、共同 XY 投影及可选择组件，原插件材质不改。保留变换后的世界 XY，将渲染 Z 投到游戏平面；同步投影渲染包围盒。碰撞、粒子模拟、逻辑中心与网络坐标不改。不逐粒子遍历，不在每次攻击实例化材质。
- 覆盖围栏/B 火焰、烈焰之笔、普通/火/毒鬼火与命中特效、Dash 箭头、近战预警、LostSoul 爆炸及已迁入提示。
- 增加被动 CPU/渲染/GPU/GC/积压记录，每秒及 ≥100 ms 长帧记录。不可用计数为 -1；不把不可用当 0。技术启动可显式固定 720p60 或 4k144，人工入口拒绝该辅助参数。
- 为六种伤害数字配置独立静态字体，保留原字符表、字形表、图集、字面指标和材质，移除数字不需要的动态 fallback。公共 UI 字体和 DamageNumbersPro 插件不改。

## 首轮自动化记录（历史）

- 修复前复现：空事件与 Animator.speed 两项失败，见 `Logs/ManualPlayFix/repro-edit.xml`。
- 首轮 EditMode：788/790。两个旧材质检查不认识平面适配；已改为核对来源 GUID、全部原参数、纹理及关键字，而非任意材质放行。
- EditMode 复验：790/790，`Logs/ManualPlayFix/edit-all-r2.xml`。
- 后台写盘：模拟磁盘阻塞、积压超限、写入失败与尾部关闭测试通过。
- PlayMode：531/531，`Logs/ManualPlayFix/play-all-r1.xml`。Sprite 和 VFX 独立材质在 FOV 80、Z=-10 的透视镜头下，世界 Z=-7/+7 的相同 XY 四边形均显示为 30×30 像素；输出 PNG 已核对。这是投影路径验证，不替代实际特效及游戏双端验收。
- 实际游戏双端、前后性能对照：进行中，未宣称通过。

最新广域结果：`edit-digits-final.xml` **791/791**，`play-final.xml` **533/533**。后者完成后追加了数字字体资产调整，再执行 `play-digits-final.xml` **2/2**（实际显示、合并、去重、池复用与清理）。不将两次运行的重复测试相加成新的广域总数。历史失败及 SHA-256 汇总保留在 `docs/evidence/manual-play-fix/test-results.json`。

## 实际画面发现的二次问题与修正

第一、二候选在真实 Host 上发现火焰移动后显示边界仍留在旧位置，导致部分火焰被错误裁剪。不能用“中心不动”或单张静态截图判定画面通过。第三候选将边界覆盖限于相机绘制阶段，结束后恢复 Unity 原生边界，粒子模拟继续更新下一帧边界；未扩大包围盒掩盖错误。

`perf-fix-barrier-pair02` 保留失败；`perf-fix-barrier-pair03` 双端完成 85 秒及界面重开。第一次 RunId 为 `90c7ee08318249e3a15069bdfaa67b19`，重开为 `d08f0afca00d4db2af35939b0e934f94`。火圈中心固定 `(-2,0)`，收缩/停止展示可见，结束时两端占用、碰撞区、预警均为零。修正前后实际粒子/边界记录见 `docs/evidence/manual-play-fix/barrier-bounds-before-after.json`；画面见 `barrier-pair03-host.png`、`barrier-pair03-client.png`。重开在第二局中途正常关闭，不能称两次完整完成。

自动化实际特效检查使用真实攻击资源，覆盖烈焰之笔及三种鬼火八方向、大小 1/2；同一帧切换原 Shader 与平面 Shader，避免把不同随机粒子形状当投影差异。围栏检查使用 NordicRenderer2D、真实地图和生产透视参数，覆盖中心 Y=0/-12、半径 10/20。此类隔离渲染不替代伤害或全流程联机验收。

测试自身另发现 Prefab 清理对内存映射文件直接写入会失败；已保留 `edit-all-r3/r4` 等失败记录，并将清理改为先恢复覆盖引用、再经 Prefab API 恢复原生命。六项定向复验通过 (`variant-r7.xml`)，随后纳入上述最终广域复验。

## 第三候选的首轮 4K 完整记录

`Builds/ManualPlayFix20260916-03`，`perf-fix03-solo4k-r1/host` 第一局 `d2b6145566d94a5aa13825edb847d9e1`：3840×2160、目标 144、D3D11，正常时间推进的 `full-validation`（89 次确定性选择、24 次测试补血、普通自动移动，非人工压力局）。实际 720.911731 秒请求并完成结算，3948 次出生属性核对无不匹配。3773 确认击杀、5 自毁、86 无经验淘汰、结束存活 84，计数守恒。结算伤害区、预警、占用、陷阱状态均为零；正常关闭后所有记录器状态 complete。

| 区间 | 每秒记录中的最大帧 ms | ≥100 ms 战斗帧 | 采样存活峰值 |
|---|---:|---:|---:|
|135–180|26.45|0|138|
|180–201|35.94|0|232|
|240–300|19.05|0|130|
|333–379|27.52|0|53|
|599–720.9|44.54|0|110|

目标 144 不代表全程 144 FPS。后半段仍有主线程约 20–40 ms 的帧，GPU/CPU 的 FrameTiming 是最近可用采样，不能逐条认定为同一长帧的耗时归因。开局战斗约 6.3/6.65/71.73 秒有 117/144/146 ms 三帧；393 ms 发生于加载等待阶段。保留原始样本，不以平均 FPS 掩盖。重开后的第二局已覆盖相同关键段，前述三条开局战斗长帧未复现；后续独立样本与离线采样仍待完成。

后半段存在 FMOD 通道抢占警告，并被 Mirror.ThreadLog 再次转发。该类警告未关闭，暂未与本次 ≥100 ms 战斗帧对应；不能把全部日志称为零警告。详细结果见 `docs/evidence/manual-play-fix/solo4k03-r1-performance.json` 与 `solo4k03-audit.json`。

显式 Unity CPU 采样入口使用官方支持的启动参数，额外开销单独标记，不混入普通帧时间比较：[Unity Profiler 命令行说明](https://docs.unity3d.com/cn/6000.0/Manual/profiler-command-line-arguments.html)。

## 冷启动短停顿的追加定位

`perf-fix03-cpu-short` 使用显式 CPU 采样（额外开销单列）。离线记录 `cpu-short-report.json` 的第 894 帧：主线程 116.15 ms，其中 `DamageNumber.Start` 为 86.68 ms；下一帧主线程 139.91 ms，其中等待渲染线程 108.34 ms。采样均为包含子调用的时间，不能相加。

伤害数字原先绑定 LiberationSans 静态字体，但该字体挂有动态 fallback。DamageNumbersPro 首次 Restart 会遍历 fallback 字符，必要时逐字符尝试生成，并创建常驻的 fallback 网格。当前伤害数字只有数字和拉丁格式符，不需要这个路径。现已用独立 `DamageDigits.asset` 复用原字形、原图集和材质，去掉数字专用配置的动态 fallback；不修改 UI 公共字体或插件。

第四候选冷启动复验：`perf-fix04-cpu-short` / `cpu-short04-report.json`，同样显式采集前 1,800 帧。首次 `DamageNumber.Start` 为 **24.93 ms**（此前 86.68 ms），对应整帧 **46.46 ms**；加载后的这段采样无 ≥100 ms 帧，先前连续两条 116/140 ms 帧不再复现。仍有首次 JIT 等成本，不能声称零开销。两份都带 CPU 采样，不与无采样的 FPS 混算。

最初 12,000 帧采样被 Unity 加载上限截为末尾 2,000 帧，不能用于证明开局原因；因此另行采集前 1,800 帧。两个原始采样均保留。

## 候选 03 期间的待办（历史）

生产透视镜头下的火圈半径/地图边缘/推拉、烈焰之笔和三种鬼火八方向及大小成长、双端窗口/死亡/复用回归。720p60 与 4K144 关键区间冷启动及重复采样，解释仍存在的秒级长帧。最终同版本单人/双端 720.9 秒与界面重开，并提供新包及哈希。

技术保护、自动输入与确定性选卡不等同于原人工局装备和操作；本轮不产生玩法压力结论。

## 第四候选 720p 完整单人运行

`Builds/ManualPlayFix20260916-04`，`perf-fix04-solo720-r1/host` 第一局 `fa934ed31bd248fea3c482a680f50f6a`：1280×720、目标 60、D3D11。720.90360088 秒请求并完成结算；2689 出生、2333 确认击杀、16 自毁、224 淘汰、结束存活 116，计数守恒，出生属性无不匹配。61 次确定性选卡、27 次测试补血与普通自动移动均有记录，不是人工压力局。结束时攻击区、预警、陷阱及占用为零。

| 区间 | 每秒记录中的最大帧 ms | ≥100 ms 战斗帧 | 采样存活峰值 |
|---|---:|---:|---:|
|135–180|20.37|0|113|
|180–201|94.64|0|205|
|240–300|20.47|0|154|
|333–379|42.48|0|189|
|599–720.9|39.55|0|126|

开局另有约 217 ms 的选卡暂停帧、约 62.4 秒首次远程攻击附近的 133 ms 帧；后者还需进一步调用采样。后半段存在约 24–26 ms 的连续帧，不声称全程 60 FPS。`solo72004-barrier-map-edge.jpg` 是实际地图下边缘的围栏画面；中心记录固定，显示压平后不改变地图限位或镜头取景。

已通过界面重开至第二轮 `a1dde241bd534226878e0ff2e293f357`，完成约 410 秒重复采样后正常关闭；这不是第二次完整通关。四个关键区间最大帧分别为 19.40、22.69、20.48、29.00 ms，没有 ≥100 ms 战斗帧。正常关闭后全部写入状态 complete。

第四候选的补充 CPU 记录 `cpu-imp04-report.json` 没有重现首轮 62.4 秒的 133 ms 战斗帧，因此目前不能归因于 Imp 初始化。该诊断局有显式 CPU 采样开销，前段还展开了调试面板，不能并入无采样性能比较。约 86 秒的 144 ms 帧位于选卡暂停，主线程主要等待渲染；当前帧与渲染流水线采样可能错位，不能将其直接解释为 GPU 渲染 144 ms。

## 第四候选双端完整运行与忙碌等待

`perf-fix04-pair720`，RunId `7d6746210a61400b899431d17ad43865`：两端同构建、720p/60，显式 `full-validation / busy`。两端均 3409 次出生，属性及完整出生记录相同；3297 确认击杀、13 自毁、45 淘汰、结算存活 54，守恒差为 0。C 成功 1139，B 成功 55；635 秒的 5 只批次因围栏占用跳过，保留来源互斥，并非漏同步。

转场请求一次，客户端保留真实选卡并测试停留约 8 秒；提交后 **729.0949935 秒**结算，主机记录请求至完成 8.1833 秒。两端伤害区/预警、陷阱状态/占用/碰撞均清零。`pair72004-client-transition.jpg` 为等待中的选卡画面，`pair72004-host-ended.jpg` 为实际结算。

通过主机结算按钮重开为 `5543224ce58c404ea5b8be76c97b8b8d`，两端恢复等级 1、初始条件及新计数。随后先正常退出客户端、再退出主机；第二轮因此存在客户端离线后的 32 次仅主机出生，不能把它混入第一轮同步差异，也不能称第二轮完整通关。所有后台文件正常关闭，审计无异常。完整证据为 `pair72004-audit.json`、`pair72004-performance.json`。

双端客户端前 143 秒曾展开开发面板，带来额外分配及约 100 ms 帧；之后两端收起。人工入口默认隐藏面板。这局用于联机与生命周期验证，不作为单进程性能基准。

## 追加定位：中文 UI 动态图集的重复上传

第四候选第三次 720p 冷启动在 65.64 秒出现 176.77 ms 战斗帧。追加显式 CPU 捕获 `perf-fix04-cpu-imp2` 重现为 69.82 秒、156.77 ms。主帧 4141 在 `Gfx.WaitForPresentOnGfxThread` 等待 143.70 ms；相邻渲染帧 4140 的 `RenderLoop` 为 162.28 ms。上传样本元数据明确指向 **REEJI-LiLing-Gothic-GB - Regular Atlas**，同一个中文字体图集在一帧被反复上传；不是 Imp／鬼火纹理。同期 UI 首次显示新选项，不能仅凭 60 秒附近就归因于 Imp。

`cpu-imp2-04-details.json` 同时保留主线程、相邻渲染帧及纹理名称。此前只保留长主帧对应渲染帧的报告缺少这一证据，现已修正离线导出方式。[Unity RawFrameDataView 元数据接口](https://docs.unity3d.com/ja/current/ScriptReference/Profiling.RawFrameDataView.GetSampleMetadataAsString.html)。

新增 `LoadedLocalizedUiPreparesGameplayTextBeforeItIsFirstRendered`：修复前 0/1，直接复现首次奖励说明缺少预备字形；修复后完整 `ContentLocalizationTests` 4/4。现有语言加载流程批量准备两张本地化表中的字符及 ASCII 格式字符，保留字体、字面指标、回退和动态未知字符路径；在发布语言就绪之前完成批量工作。不修改原字体资产，不在战斗逐帧扫描文本。新候选仍须运行采样和全流程验证，04 的完整运行不能代替新包验收。

该修复后完整 PlayMode 为 **534/534**（`play-ui-final.xml`，实际测试时长 402.36 秒）。显式性能配置随后使用与人工入口相同的“关闭开发面板”条件，避免每次依赖前台 F2/F3；普通专项验证入口维持原行为。

同一代码版本的完整 EditMode 为 **791/791**（`edit-ui-final.xml`）。两份结果均已归档哈希。测试期间字体缓存和 NetworkEnemyBase 的四个缺省序列化字段已恢复，不进入候选 06；没有把测试缓存当作预烘焙字体交付。

候选 05 为临时诊断构建，启动后 Windows 防火墙提示遮挡画面，且开发面板未收起；之后又与后台广域测试重叠，**不计正式性能或画面通过**。该候选还带有编辑器测试生成的英文字体特征缓存，最终构建前将恢复这份非意图资产变化。记录保留，不拿其运行状态代替最终构建。

## 最终候选 06：4K 完整单人验证

候选 `ManualPlayFix20260916-06` 包含批量 UI 字形准备及性能入口关闭开发面板；构建后未再修改运行代码或资源。最终 EditMode 791/791、PlayMode 534/534。

`perf-fix06-solo4k-final` 的完整局 `2549256b059f4583b8edc13dd8dd6ae6` 使用实际 3840×2160、目标 144、D3D11，720.9144366 秒请求并完成。4260 次成功出生、4179 确认击杀、8 自毁、3 淘汰、结束存活 70，守恒差 0；出生属性零不匹配。C 成功 1138（末段相对等待截至结算尚余 1），B 成功 55（一次 5 只批次被围栏占用跳过），未修改来源预算或增加追赶补发。95 次确定性选卡、18 次测试补血及普通自动移动均有记录。

|区间|最大窗口帧 ms|≥100 ms 战斗帧|
|---|---:|---:|
|135–180|17.01|0|
|180–201|19.73|0|
|240–300|22.36|0|
|333–379|29.40|0|
|599–720.9|41.79|0|

此前约 60–80 秒的中文图集上传长帧未在本次重现。加载阶段仍有 389 ms 帧，刚进入 Running 但关卡时间仍为 0 的首帧为 128.79 ms；保留在明细，不称零长帧。目标 144 不代表全程达到 144 FPS，后半段 CPU 帧时间仍有约 20–30 ms。

三类已修复警告（空动画事件、无效动画速度、Static 刚体速度写入）均为 0；FMOD 通道抢占等其他警告仍保留。所有记录器正常关闭，无失败/溢出。结算伤害区、预警、陷阱状态及占用均为 0。通过界面重开为 `0f410b4912a24f6fa2b139ef2a6baf55`，核对生命 500、等级 1、时间/计数清零后正常退出。

实际画面：`solo4k06-first-barrier.jpg` 为 342.1 秒；`solo4k06-second-barrier.jpg` 为 615.6 秒，均可见平面火焰。`solo4k06-elapsed269.jpg` 为史莱姆开始之前的强化骷髅画面，不作为 285 秒史莱姆证据。原始人工局与技术局操作、装备不同，不据此宣称同条件玩法难度或精确性能提升倍数。

## 围栏暂停检查的快照延迟误报（候选 06 保留失败）

`perf-fix06-barrier-pause`：Building 和 Shrinking 的暂停断言失败；战斗时间完全不变，发布状态分别补到最后 1 组火焰、最后约 0.006667 半径步进。`TickReferenceTrapAuthority` 以通信时间每 0.05 秒发布，而验证器原先比较 SyncDictionary 的旧值与新值。既有原生围栏暂停测试直接读取对象，不存在这层发布延迟。

验证代码现改为直接读取同一个实际围栏的阶段、可见组数、半径与碰撞，同时单独记录 `publishedStates`；没有增加等待、容差，没有强制发布，也没有改变暂停/收缩规则。新增真实 BarrierTrap 的测试，验证迟到快照不改变实际冻结判断。修复后的现场复验尚待完成，06 的这两项不会改写成通过。

06 全流程和 B 暂停均已完成；此后只有显式空间验证器及对应测试发生变化。为保持交付记录准确，将生成独立 07 候选，再执行受影响的隔离验收及最终版本完整流程。

## 候选 06：双端完整验证与重开

`perf-fix06-pair720-final`，完整局 `789b989d2efe4a078ef54540bcbcad24`：两端均为 3738 出生、3626 确认击杀，完整出生内容零差异；23 自毁、30 淘汰、结束存活 59，计数守恒。一次 B 受占用跳过，其余按来源语义执行。转场请求一次，真实客户端选卡等待约 8.2 秒后，两端于 **729.09762923 秒**共同结算。攻击区/预警、陷阱/占用清零。

主机通过结算界面重开为 `736569d8af144dd2a5702188d9b602ea`，两端初始条件和等级 1 已核对。随后正常先退出 Client、再退出 Host；重开局中客户端离线后的额外主机出生不能算作完整局同步缺失。两端全部后台记录正常关闭。

实际画面：Host 第一次围栏 360.9 秒、Client 第一次围栏 343.3 秒；Host 第二次围栏约 625.5 秒。Client 后一次截图实际为 645.4 秒，围栏已释放，已改名 `pair72006-client-elapsed645.jpg`，不将其作为第二围栏显示证据。转场抓图到达时已经完成，文件为 `pair72006-client-ended.jpg`；等待与提交由两端日志核对。

## 候选 06：B 暂停与展示

`perf-fix06-b-pause`，`58f80e60440c487f9932db08c40fb5b6`。Delay/Building/Stopping 暂停检查均通过，两端 5 次出生内容一致；暂停期间时钟和固定中心不变，结算释放占用。`b06-client-warning.png` 来自该真实游戏 Client 的阶段截图，已查看，五处火焰处于同一显示平面。手工捕获的 `b06-client.jpg` 实际为结算画面；Host 暂停画面另存 `b06-host-paused.jpg`。

围栏原生生命周期及新的快照延迟检查定向测试 **3/3** (`trap-pause-observation.xml`)；这是已通过广域 534 项之外的定向复验，不合并重复计数。候选 07 构建中，尚未完成其画面复验。当前游戏窗口无法被截图工具捕获，已请求恢复窗口；不把不可捕获的状态当作画面通过。

## 候选 07：空间暂停与取消复验

构建结果：`Logs/ProjectTools/20260916-173343-520-build.player/result.json`，成功。相对 06 仅修正显式空间验证取样和新增对应测试，游戏运动、显示、碰撞与暂停实现未变。

`perf-fix07-barrier-pause` / `5d195da957924784813711821c98e9c6`：Delay、Framing、Building、Shrinking、Stopping 的暂停全部通过。Shrinking 开始时实际半径 19.74661255，发布值 19.75328064；暂停结束实际半径仍为 19.74661255，发布值才追上。这直接复核了 06 的快照延迟误报，未放宽断言。85 秒后两端状态、占用、碰撞及预警清零，正常关闭。

`perf-fix07-barrier-cancel` / `0ac08ebaff2a4d3aa1cf33561ebfdd26`：在 Shrinking 暂停后取消。战斗时间保持 11.45184158，取消后陷阱为空、暂停仍为 0；仅显式 Resume 后恢复 1。Client 5.2403277 秒进入取消结算，伤害/预警/碰撞/占用为零。两端正常关闭；取消不是完成 85 秒正常生命周期。

旧窗口暂时不可截图已绕过：新窗口可正常捕获，旧验证进程经现有窗口键盘关闭入口正常退出，并非强制杀进程。07 的最后单人 4K 全流程已启动，尚未完成，不沿用 06 的全流程结论。


## 候选 07：4K 单人完整流程与重开

`perf-fix07-solo4k-final`，`0c72117b999940f48c6edb8954ed69a1`：实际 3840×2160、目标 144，720.90273454 秒请求并完成。4207 次出生、4112 确认击杀、1 自毁、7 淘汰、结束存活 87，守恒差 0；出生属性零不匹配。C 成功 1138、B 55；末段 C 尚余 1 个相对等待机会，一次 5 只 B 被占用跳过。92 次确定性选卡和技术保护明确记账。

135–180、180–201、240–300、333–379、599–720.9 秒最大窗口帧分别为 **15.98、24.78、18.04、30.93、35.85 ms**，这些区间无 ≥100 ms 战斗帧。加载约 381 ms，刚进 Running 且关卡时间为 0 的首帧约 127 ms，未从原始记录删除；不宣称全程 144 FPS。三类已修复警告为 0，其他音频警告仍保留。

结束攻击区、预警、陷阱和占用为 0。通过结算按钮重开为 `d1452b0544194a5891cfc77698664107`，等级 1、生命 500、时间和计数清零；约 15 秒正常退出，不将重开局称为完整完成。全部后台文件状态 complete。证据：`solo4k07-audit.json`、`solo4k07-performance.json`、`solo4k07-ended.jpg`、`solo4k07-restart.jpg`。本次手动捕获错过第二围栏时段，不伪称已有对应截图；双端最终局仍需补充。


## 候选 07：双端最终完整流程与重开

`perf-fix07-pair720-final`，`7cb69645e41347a3a0d702670652f019`。两端 4017 次出生内容相同，属性零不匹配；3951 确认击杀、31 自毁、1 淘汰、结束存活 34，守恒差 0。C 成功 1139、B 55，一次 5 只 B 因占用跳过。

720.90009152 秒请求；等待真实选卡约 8.19 秒，提交后两端在 **729.08669522 秒**结算。`pair72007-client-transition.jpg` 是约 724 秒的实际等待画面，显示两个存活玩家正在选卡；不是结算后补拍。结束攻击区、预警、陷阱/占用/碰撞清零。

主机通过界面重开为 `36714352ea9f4700a53c1b68aaa11fc9`；两端新 RunId、生命 500、初始武器及等级 1 已核对。Client 重开截图实际为约 52 秒、已升至等级 2，不能把截图本身称为等级 1 证据；初始值由初始化日志证实。随后依次正常退出两端，所有后台文件 complete、审计错误为空。比较结果中重开局客户端退出后的 35 次主机出生不属于完整局同步错误。

第一围栏中心两端均固定 `(7.4378109,-16.4517536)`；第二为 `(1.3490918,-13.7755108)`，无跟随玩家变化。两次围栏均有 Host/Client 画面，第二次分别约 623.5/632.7 秒。半径仍保留来源逐帧终值，碰撞未随美术修改。完整局两端均无关卡时间大于 2 秒的 ≥100 ms 战斗帧，其他音频警告仍保留。证据见 `pair72007-audit.json`、`pair72007-performance.json`。
